# dependencies
node_modules
.pnp
.pnp.js

# next
.next
build
next-env.d.ts
# `out/` is intentionally NOT gitignored — the autodeploy at
# demo.gabrielgabrie.com/thecivil is a static file host that serves whatever
# is checked into the repo. The built site lives in out/. Run `npm run build`
# to refresh it before pushing.

# production
dist

# testing
coverage

# environment
.env*.local
.env.local
.env.development.local
.env.production.local

# misc
.DS_Store
*.pem
.idea
.vscode
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo

# claude code
.claude/settings.local.json

# scratch
plans/_scratch/
# Original scraped photos — duplicates of the labelled copies in public/images/.
# See ASSETS.md for the rename mapping; re-downloadable via the GoDaddy CDN.
public/images/source/
